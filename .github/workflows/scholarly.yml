name: Scholarly Check

on:
  push:
    branches: [ "scholarly-v1", "main" ]

jobs:
  check:
    runs-on: ubuntu-latest

steps:
  - name: Checkout repository
    uses: actions/checkout@v4

  - name: Run placeholder check
    run: python3 scripts/check_placeholders.py
