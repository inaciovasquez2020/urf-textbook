\begin{lemma}[Width--Entropy Bridge]
\textbf{Dependencies: } D1, D3, D5
\textbf{Status: } Proven
Let a refinement step reduce entropy by $\delta > 0$ under $\mathrm{FO}^k$-local reasoning of width $w$.
Then there exists a constant $c_k > 0$ such that
w
\geq
c
k
log
⁡
 ⁣
(
1
δ
)
.
w\geqc 
k
​	
 log( 
δ
1
​	
 ).
In particular, bounded-width refinement cannot induce large entropy drops.
\end{lemma}
